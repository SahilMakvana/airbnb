<% layout("/layouts/boilerplate.ejs") -%>
    <body>
        
        <form class="needs-validation" method="POST" action="/listings/<%= listing._id %>?_method=PUT" novalidate>
            <div class="row">
                <div class="col-8 offset-2">
                    <h3 class="mb-3">Edit Listing</h3>
                    <div class="mb-3">
                        <label for="title" class="form-label">Title</label>
                        <input name="title" class="form-control" type="text" id="title" placeholder="enter title" value="<%= listing.title %>" required/>
                        <div class="invalid-feedback">Please enter a Title</div>
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <textarea name="description" class="form-control" id="description" placeholder="enter description" rows="3" required/><%= listing.description %></textarea>
                        <div class="invalid-feedback">Please enter a Description</div>
                    </div>
                    
                    <div class="mb-3">
                        <label for="image" class="form-label">Image</label>
                        <input name="image" class="form-control" type="text" id="image" placeholder="enter image url" value="<%= listing.image %>" required/>
                        <div class="invalid-feedback">Please enter a valid Image</div>
                    </div>

                    <div class="mb-3 row">
                        <div class="col-4">
                            <label for="price" class="form-label">Price</label>
                            <input name="price" class="form-control" type="number"  id="price" placeholder="enter price" value="<%= listing.price %>" required/>
                            <div class="invalid-feedback">Please enter a valid Price</div>
                        </div>
                        <div class="col-8">
                            <label for="country" class="form-label">Country</label>
                            <input name="country" class="form-control" type="text" id="country" placeholder="enter country" value="<%= listing.country %>" required/> 
                            <div class="invalid-feedback">Please enter a valid Country</div> 
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        <label for="location" class="form-label">Location</label>
                        <input name="location" class="form-control" type="text" id="location" placeholder="enter location" value="<%= listing.location %>" required/>
                        <div class="invalid-feedback">Please enter a valid Location</div>
                    </div>
                    <button class="btn btn-dark">Edit Listing</button>
                    <br>
                    <br>
                </div>
            </div>
        </form>
        <!-- <form method="POST" action="/listings/<%= listing._id %>?_method=PUT">
            <input name="title" type="text" placeholder="enter title" value="<%= listing.title %>" />
            <br /><br />
            <textarea name="description" rows="5"><%= listing.description %></textarea>
            <br /><br />
            <input name="image" type="text" placeholder="enter image url" value="<%= listing.image %>" />
            <br /><br />
            <input name="price" type="number" placeholder="enter price" value="<%= listing.price %>" />
            <br /><br />
            <input name="location" type="text" placeholder="enter location" value="<%= listing.location %>" />
            <br /><br />
            <input name="country" type="text" placeholder="enter country" value="<%= listing.country %>" />
            <br /><br />
            <button>Edit</button>
        </form> -->
    </body>
